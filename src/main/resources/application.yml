spring:
  profiles:
    group:
      default:
        - local
  datasource:
    url: jdbc:sqlserver://127.0.0.1:1433;encrypt=false;database=databaseName;
    username: sa
    password: sa123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect